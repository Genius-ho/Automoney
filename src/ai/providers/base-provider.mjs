export const CAPABILITIES=Object.freeze(['text_generation','vision_analysis','image_generation','image_edit']);
export function createProvider({id,displayName,capabilities}){const supported=new Set(capabilities);const disabled=async capability=>{if(!supported.has(capability))throw providerError('CAPABILITY_UNSUPPORTED',`${displayName} does not support ${capability}`);throw providerError('NOT_IMPLEMENTED','Paid AI operations are disabled in this release');};return Object.freeze({id,displayName,capabilities:[...supported],validateConfig(config={}){const errors=[];if(config.baseUrl){try{new URL(config.baseUrl)}catch{errors.push('baseUrl must be a valid URL')}}return{valid:errors.length===0,errors};},async testConnection(config={}){const result=this.validateConfig(config);return result.valid?{status:'not_tested',message:'Configuration is valid; no external request was made'}:{status:'configuration_invalid',message:result.errors.join('; ')};},listConfiguredModels(config={}){return[config.defaultTextModel,config.defaultVisionModel,config.defaultImageModel].filter(Boolean);},generateText:()=>disabled('text_generation'),analyzeImages:()=>disabled('vision_analysis'),generateImage:()=>disabled('image_generation'),editImage:()=>disabled('image_edit')});}
export function providerError(code,message){const error=new Error(message);error.code=code;return error;}
